<?xml version="1.0" encoding="utf-8"?>
<manifest>
  <control namespace="PCF" constructor="AdvisorCockpit" version="1.0.3" display-name-key="CRMShow_AdvisorCockpit" description-key="Advisor Cockpit page-level dashboard (Sprint-003 #62/#64/#122)" control-type="standard">
    <!--
      Standard (not virtual) control-type: this control bundles its own React 18 +
      Fluent UI v9 runtime (see index.ts) instead of using the platform-provided
      React 16 library, because the already-built, already-tested component in
      ../src/AdvisorCockpit.tsx (merged PR #70, 24/24 vitest) uses React 18 APIs.
      Reusing that component as-is (per the polish-loop pattern) requires bundling
      React 18 rather than adopting the older platform-library version.
    -->
    <external-service-usage enabled="false" />
    <!--
      The model-driven Contact form binds this host property to nickname.
      The control currently renders its synthetic cockpit fixtures internally;
      the bound field provides the supported form placement and future record
      context seam.
    -->
    <property name="boundData" display-name-key="Advisor Cockpit host field" description-key="Dataverse text field used to host the Advisor Cockpit control on a model-driven form." of-type="SingleLine.Text" usage="bound" required="false" />
    <resources>
      <code path="index.ts" order="1" />
    </resources>
  </control>
</manifest>